import { IEnvironment } from '@environments/ienvironment';

export const environment: IEnvironment = {
  production: true,
  apiUrl: 'https://halo-achievement-tracker-app-api.azurewebsites.net/api'
};
